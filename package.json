{
    "name": "keroro-cli-monorepo",
    "version": "1.0.0",
    "license": "ISC",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "publish": "pnpm public -r",
        "format": "prettier --write \"packages/**/*.{js,ts,jsx,tsx,json,md}\"",
        "prepare": "husky install",
        "build": "lerna run build"
    },
    "lint-staged": {
        "*.{js,ts,jsx,tsx,json,md}": "prettier --write"
    },
    "devDependencies": {
        "husky": "^9.1.7",
        "lerna": "^8.2.2",
        "lint-staged": "^15.5.2",
        "prettier": "^3.5.3"
    },
    "dependencies": {
        "axios": "1.9.0",
        "simple-git": "3.27.0"
    }
}
