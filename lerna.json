{
    "version": "independent",
    "useWorkspaces": true,
    "command": {
        "publish": {
            "conventionalCommits": true,
            "message": "chore(release): beta publish",
            "ignoreChanges": ["**/*.md", "*.md"],
            "allowBranch": "master"
        }
    }
}
